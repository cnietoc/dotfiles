[project]
name = "update-router-dhcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "notion-client>=3.1.0",
    "paramiko>=5.0.0",
]
